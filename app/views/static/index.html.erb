

<div class="container">

<h1>Bonjour cher visiteur</h1>

<% Gossip.all.each do |gossip| %>

<div class="card">
  <div class="card-header">
    POTIN n°<%= gossip.id %>
  </div><br><br>
  <div class="card-body">
    <h5 class="card-title">L'auteur : <%=  gossip.user.first_name%></h5>
    <p class="card-text">Le titre : <%=  gossip.title%></p>
    <a href="home/<%= gossip.id %>" class="btn btn-primary">Clique pour voir la page du potin </a> <br><br><br><hr/>
  </div>
</div>
<% end %>



</div>
